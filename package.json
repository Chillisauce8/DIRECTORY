{
  "name": "directory",
  "private": true,
  "workspaces": [
    "base",
    "car"
  ],
  "scripts": {
    "dev:base": "cd base && npm run dev",
    "dev:car": "cd car && npm run dev",
    "build:base": "cd base && npm run build",
    "build:car": "cd car && npm run build"
  },
  "devDependencies": {
    "eslint": "^9.21.0",
    "husky": "^9.1.7",
    "prettier": "^3.5.2"
  }
}
