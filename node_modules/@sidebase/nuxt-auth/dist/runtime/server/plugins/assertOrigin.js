import { ERROR_MESSAGES } from "../services/errors.js";
import { isProduction } from "../../helpers.js";
import { getServerOrigin } from "../services/utils.js";
function defineNitroPlugin(def) {
  return def;
}
export default defineNitroPlugin(() => {
  try {
    getServerOrigin();
  } catch (error) {
    if (!isProduction) {
      console.info(ERROR_MESSAGES.NO_ORIGIN);
    } else {
      throw error;
    }
  }
});
